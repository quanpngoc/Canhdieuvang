@{
    //WebSecurity.InitializeDatabaseConnection("Users", "UserProfile", "UserId", "Email", true);
    //The name the SMTP server that will be used to send the emails.
    WebMail.SmtpServer = "smtp.gmail.com";
    //The port the server will use to send SMTP transactions (emails).
    WebMail.SmtpPort = 465;
    //True, if the server should use SSL (Secure Socket Layer) encryption.
    WebMail.EnableSsl = false;
    //The name of the SMTP email account used to send the email.
    WebMail.UserName = "tranhungquang23497@gmail.com";
    //The password of the SMTP email account.
    WebMail.Password = "quangprono2";
    //The email to appear in the from address (often the same as UserName).
    WebMail.From = "contact@canhdieuvang.com";
}